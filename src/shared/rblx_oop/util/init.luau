local Comparison = require(script.comparison)
local Function = require(script["function"].THIS)
local Vector = require(script.Vector)

return {
    Function = Function,
    Comparison = Comparison,
    Vector = Vector
}